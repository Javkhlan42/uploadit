# 🚀 Quick Start Guide - AI Assistant

## Setup хийх (5 минут)

### 1. Dependencies суулгах
```bash
npm install
```

### 2. Environment тохируулах
```bash
# .env файл үүсгэх (хэрэв байхгүй бол)
cp .env.example .env

# .env дотор GEMINI_API_KEY байгаа эсэхийг шалгах
# Аль хэдийн default key бүхий .env файлтай байх ёстой
```

### 3. Database migration
```bash
npx prisma generate
npx prisma migrate dev --name add_embedding_to_yellowbook
```

### 4. Embeddings үүсгэх (ЗААВАЛ)
```bash
npm run ai:embed
```

**Энэ алхам их чухал!** Өгөгдлийн санд бүх бизнесүүдийн embedding үүсгэнэ.
Хүлээх хугацаа: ~2-5 минут (150 бизнестэй бол)

### 5. Серверүүдийг асаах

```bash
# Terminal 1: API
npm run start:api

# Terminal 2: Frontend (өөр terminal дээр)
npm run start:web
```

### 6. Хөтөч нээх

```
http://localhost:4200/assistant
```

## 🧪 Тест хийх

### Browser дээр:
1. `http://localhost:4200/assistant` хаяг руу очих
2. Монгол хэлээр асуулт бичих, жишээ нь:
   - "Төв дүүрэгт сайн ресторан байна уу?"
   - "Сүхбаатар дүүрэгт банк хаана байдаг вэ?"
3. "AI-аар хайх" товч дарах
4. 2-3 секунд хүлээх
5. AI хариулт + 5 газрын жагсаалт харагдана

### Terminal дээр (API шууд тест):
```bash
# Windows PowerShell
$body = @{
    question = "Сүхбаатар дүүрэгт сайн ресторан байна уу?"
    limit = 3
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:3333/api/ai/yellow-books/search" `
    -Method Post `
    -ContentType "application/json" `
    -Body $body
```

## 📊 Системийн шалгалт

### 1. Embedding шалгах
```bash
# Prisma Studio нээх
npx prisma studio

# YellowBook table-ийн embedding field-д өгөгдөл байгаа эсэхийг шалгах
```

### 2. API endpoint шалгах
```
GET http://localhost:3333/api
```
Хэрвээ ажиллаж байвал `200 OK` + JSON хариулт ирнэ.

### 3. Redis шалгах (optional)
```bash
redis-cli ping
# Хэрвээ "PONG" гэсэн хариулт ирвэл Redis ажиллаж байна
# Redis алдаа өгсөн ч систем ажиллана (cache-гүй горимд)
```

## ❌ Түгээмэл алдаанууд

### "No businesses found"
→ **Шийдэл**: `npm run ai:embed` ажиллуулж embedding үүсгэх

### "Quota exceeded" (embedding үүсгэх үед)
→ **Шийдэл**: 1-2 минут хүлээгээд дахин оролдох

### "CORS error" (browser console)
→ **Шийдэл**: `.env` дээр `NEXT_PUBLIC_API_URL=http://localhost:3333` байгаа эсэхийг шалгах

### "Redis connection refused"
→ **Шийдэл**: Санаа зовох хэрэггүй! Redis байхгүй ч систем ажиллана (cache-гүй горимд)

## 🎯 Demo хийх

1. **Энгийн хайлт**:
   - Асуулт: "Төв дүүрэгт ресторан байна уу?"
   - Үр дүн: 5 ресторан + AI хариулт

2. **Filter ашиглах**:
   - Асуулт: "Сайн газар санал болгоно уу?"
   - Дүүрэг: "Сүхбаатар"
   - Ангилал: "Ресторан"

3. **Cache test**:
   - Нэг асуултыг 2 удаа асуух
   - Хоёр дахь удаагийн хариулт хурдан ирнэ + "⚡ Кэш" badge харагдана

## 📱 Гар утас дээр үзэх

1. IP хаягаа олох:
```bash
ipconfig
# IPv4 Address харах (жишээ: 192.168.1.100)
```

2. .env засах:
```env
NEXT_PUBLIC_API_URL=http://192.168.1.100:3333
```

3. Гар утасны browser:
```
http://192.168.1.100:4200/assistant
```

## 🎉 Амжилттай!

Одоо та:
- ✅ AI Assistant ажиллаж байна
- ✅ Монгол хэл дэмжих
- ✅ Semantic search + RAG
- ✅ 5 топ бизнес олж өгнө
- ✅ Cache ашиглаж хурдасгана

**Дараагийн алхам**: [AI_ASSISTANT_README.md](./AI_ASSISTANT_README.md) уншиж дэлгэрэнгүй мэдээлэл авах
